package app.editors.manager.managers.services

/**
 * FOSS stub: MessageService converted from FirebaseMessagingService to a plain Service.
 * Push notifications via Firebase Cloud Messaging are no longer supported.
 * The service is kept as an empty stub to avoid breaking AndroidManifest references.
 */
class MessageService : android.app.Service() {

    override fun onBind(intent: android.content.Intent?): android.os.IBinder? {
        return null
    }

}
